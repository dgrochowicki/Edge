# Decyzja: freeze metody v2 i porządki w repo

**Data:** 2026-09-23 · **Status:** zatwierdzone przez operatora 2026-09-23 (plik przeniesiony z `docs/2026-09-23-v2-freeze.md`)
**Dokument metody:** `docs/METHOD_V2.md` (zamrożona)
**Poprzednia decyzja:** `docs/decisions/2026-09-19-v1-checkpoint-150.md`

## 1. Co zostało uzgodnione między agentami

Siedem otwartych punktów z `METHOD_V2_GPT_RESPONSE_TO_CLAUDE.md` — **wszystkie zamknięte, claude akceptuje**:

| # | Punkt | Stanowisko claude |
|---|---|---|
| 1 | Jedna kanoniczna próbka v2 zamiast osobnych gpt-v2 i claude-v2 | akceptuję |
| 2 | gpt jako główny analityk dzienny, claude jako okresowy audytor | akceptuję, z jednym warunkiem (niżej) |
| 3 | Zrewidowane kryteria promocji | akceptuję |
| 4 | BET i CLV bez roli w promocji v2.0 | akceptuję |
| 5 | Uproszczona kara rosterowa (tylko stand-in, −2 pp) | akceptuję |
| 6 | Reguła pokrycia 80% dla wyników rundowych | akceptuję |
| 7 | Rezygnacja z równoległego v1 na żywo | **akceptuję — wycofuję swoją wcześniejszą rekomendację** |

**Do punktu 7, bo to była moja teza.** gpt pytał o konkretną korzyść statystyczną z żywego v1, której nie daje porównanie v2 z bieżącym rynkiem. Nie ma takiej korzyści. v1 okazało się nieodróżnialne od rynku, więc „v2 vs v1" niesie tę samą informację co „v2 vs rynek", tylko drożej. Dodatkowo, gdyby oba liczył jeden wykonawca, v1 przestałoby być niezależnym baseline'em — o czym sam pisałem tydzień temu przy pytaniu o sugerowanie się. Zostaje: v1 zamrożone jako archiwum, v2 jedyną liczoną metodą, rynek jedynym benchmarkiem.

**Warunek do punktu 2.** Audyt ma sens tylko wtedy, gdy da się przeliczyć wynik od zera. To wymaga zapisywania **całego okna formy** (lista map z datami, przeciwnikami, pasmem rankingowym i wynikiem rundowym), a nie samego `fresh_map_score`. Stąd `data/form/YYYY-MM-DD.json` w sekcji 15 metody. Bez tego audytor tylko czyta cudze podsumowanie.

**Jedna rzecz dodana przeze mnie, wymaga Twojej zgody:** reguła zatrzymania (sekcja 20 metody). Jeśli przy 150 predykcjach przedział ufności zawiera zero, a jest już wąski (połowa szerokości ≤ 0.003), ogłaszamy „brak wykrywalnej przewagi" i kończymy. Bez tego v2 może wisieć latami w stanie „jeszcze nie wiadomo".

**Sprostowanie audytu 23.09 (`V2_FREEZE_TASKS.md` §0.3).** Audyt raportował 26 wpisów v1 z rozjazdem `p_est` vs `round(1/fair_odds, 4)` (25 claude, 1 gpt). Opiekun księgi przeliczył to na `data/bets.json` i wyszło **23, wszystkie claude**, próg 0.0001, maks. rozjazd 0.0028 (zgodny z audytem). **Decyzja Doma:** "przyjmij próg 0.0001 i liczbę 23" — liczba 23 jest kanoniczna, 26 było błędem audytu i nie jest już używane w dokumentacji (`docs/archive/METHOD_V1.md` §7, walidator w `scripts/validate_bets.py`).

## 2. Decyzje operatora (podjęte 2026-09-23)

- [x] **Zatwierdzenie `METHOD_V2.md`** jako wersji do zamrożenia. **Decyzja Doma:** tak — "tak, jedziemy, zadania 2–10".
- [x] **Reguła zatrzymania** z sekcji 20. **Decyzja Doma:** zgoda, w ramach zatwierdzenia całej metody.
- [x] **Data freeze i commit SHA.** **Decyzja Doma:** "freeze datuj na commit kroku 4" — czyli commit `docs: PLAYBOOK / ROADMAP / REPORT_TEMPLATE for v2` z sekcji 11 `V2_FREEZE_TASKS.md`. SHA i data wpisane w nagłówek `docs/METHOD_V2.md` po wykonaniu tego commita (nie mogły być znane w chwili jego tworzenia).
- [x] **Pierwszy kwalifikujący się event.** **Decyzja Doma:** "według reguły, nie nazwy". Sprawdzone: **PGL Fall 2026 jest S-Tier / Valve Tier 1, 1–11.10.2026, 16 drużyn** (Liquipedia, przez wyszukiwanie web 23.09 — bezpośredni fetch Liquipedia zablokowany, więc nie zweryfikowane z pierwszej ręki na stronie). Zaczyna się przed ESL Pro League S24 (3–11.10), więc to on jest pierwszym kwalifikującym się eventem, nie ESL Pro League jak wstępnie zakładano. Kwalifikacje do PGL Fall (NA/EU) zostają poza zakresem jak każda kwalifikacja.
- [x] **Realne pieniądze:** pozostają wstrzymane dla obu wersji (v1 i v2). **Decyzja Doma:** potwierdzone, plus wyjaśnienie: kupony "for fun" Doma (`source: "user_bet"` w `data/bets.json`) są niezależne od statusu metody agentów i grane dalej jak dotąd — to nie są realne pieniądze *na BET-ach agentów*, więc ten wstrzymanie ich nie dotyczy. Spójne z decyzją z 19.09.
- [x] **Twoja nowa rutyna dzienna — closing po obu stronach przy każdym meczu.** **Decyzja Doma (potwierdzona 2026-09-23, po dopytaniu):** "tak będę się starał dostarczać closing odds, możemy czasem nie mieć tych kursów". Best-effort, nie pełne pokrycie — zgodnie z ustaleniem z 19.09. Brak odczytu zostaje `null` na zawsze, nigdy nie jest uzupełniany po znanym wyniku. Priorytet przy ograniczonym czasie: mecze z największym `fresh_adj`/`roster_adj` i ewentualne BET-y, potem reszta. §18 metody zawsze raportuje pokrycie jawnie, więc częściowe pokrycie obniża moc metryki ruchu linii, ale jej nie unieważnia.

## 3. Porządki w repo

**Do usunięcia po scaleniu `METHOD_V2.md`** — cała treść tych plików jest w metodzie albo w logu decyzji, a historia git je zachowa:

| Plik | Powód |
|---|---|
| `docs/METHOD_V2_GPT_DRAFT.md` | wchłonięty przez METHOD_V2.md |
| `docs/METHOD_V2_CLAUDE_REVIEW.md` | wchłonięty; ustalenia w logu decyzji, sekcja 25 |
| `docs/METHOD_V2_GPT_RESPONSE_TO_CLAUDE.md` | wchłonięty |
| `docs/Edge — V2 Research & Rollout Specification.md` | opisuje model „v1 active + v2 shadow", który właśnie odrzuciliśmy |
| `docs/PRE-V2.md` | notatnik roboczy; jego zadaniem było doprowadzić do freeze, co się stało. Pomysły niewykorzystane w v2.0 przeniesione do sekcji 24 metody |

To jest ten sam wzorzec, co przy wcześniejszych sprzątaniach w projekcie: pliki jednorazowe znikają po wykonaniu, trwałym zapisem jest PROJECT_MEMORY.

**Do zachowania bez zmian:** `METODA.md` (wyjaśnia de-vig i value, nadal aktualne), `data/`, `reports/`, `dashboard/`.

**Do aktualizacji — mogę zrobić w następnym kroku:**

| Plik | Co wymaga zmiany |
|---|---|
| `PLAYBOOK.md` | sekcja o wersjach metody: v1 zamrożone, v2 aktywne, odesłanie do METHOD_V2.md; scope i staking bez zmian |
| `REPORT_TEMPLATE.md` | szablon raportu v2: jedna wycena, pola z sekcji 16 metody, kurs zamknięcia po obu stronach |
| `ROADMAP.md` | zamknięcie fazy v1, start fazy v2 |
| `PROJECT_MEMORY.md` | wpis: freeze v2, co się zmieniło, jaki werdykt to wywołał |
| `data/bets.json` | schemat: nowe pola v2 plus `closing_odds_opponent` |
| `dashboard/` | metryki per `agent × method_version`, nowa metryka ruchu linii |

## 4. Czego nie robimy

- Nie dobieramy parametrów v2 (0.5 × FreshDiff, half-life, capy) pod dane v1.
- Nie liczymy v2 wstecz na meczach v1.
- Nie uznajemy v2 za lepsze dlatego, że v1 nie przeszło checkpointu. v1 nie przegrało z v2 — v1 nie dało się odróżnić od rynku i to samo może spotkać v2.
